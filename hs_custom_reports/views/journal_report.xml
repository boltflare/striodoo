<?xml version='1.0' encoding='utf-8'?>
<odoo>
	<data>

		<template id="line_template_journal_report" inherit_id="account_reports.line_template" primary="True">
			<xpath expr="//span[hasclass('o_account_reports_caret_icon')]" position="before">
				<t t-if="line.get('trust') == 'good'">
					<span class="fa fa-circle color-green trust-partner" role="img" aria-label="Partner ledger is good" title="Partner ledger is good"></span>
				</t>
				<t t-if="line.get('trust') == 'bad'">
					<span class="fa fa-circle color-red trust-partner" role="img" aria-label="Partner ledger is bad" title="Partner ledger is bad"></span>
				</t>
				<t t-if="line.get('trust') == 'normal'">
					<span class="fa fa-circle color-transparent trust-partner" role="img" aria-label="Partner ledger is normal" title="Partner ledger is normal"></span>
				</t>
			</xpath>
			<xpath expr="//div[hasclass('o_account_reports_domain_dropdown')]/*[last()]" position="before">
				<t t-if="line['caret_options'] != 'account.tax'" groups="account.group_account_user">
					<a role="menuitem" tabindex="-1" t-att-data-id="line['id']" action="open_document" class="dropdown-item">View Journal Entry</a>
				</t>
			</xpath>
		</template>

		
		<record id="action_account_report_stri_journal" model="ir.actions.client">
			<field name="name">Journal Report</field>
			<field name="tag">journal_report</field>
			<field name="context" eval="{'model': 'account.stri.journal.report'}" />
		</record>
		

		<menuitem id="menu_action_account_report_stri_journal" name="Journal Report" action="action_account_report_stri_journal" parent="account_reports.account_reports_partners_reports_menu" groups="account.group_account_user"/>
	</data>
</odoo>